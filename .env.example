# Copy this file to backend/.env if you need to override any defaults.
# For a standard local or VPS setup with Ollama, no env vars are required.

# Optional: override SQLite database path (relative to backend/ directory)
# DATABASE_URL=sqlite+aiosqlite:///./helveti.db
